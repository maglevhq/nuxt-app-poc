{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev --port=3001",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build-maglev-gen": "./node_modules/.bin/esbuild cli/maglev-gen.ts --bundle --platform=node --outfile=./bin/maglev-gen",
    "build-maglev-sync": "./node_modules/.bin/esbuild cli/maglev-sync.ts --bundle --platform=node --outfile=./bin/maglev-sync",
    "maglev-gen": "./bin/maglev-gen",
    "maglev-sync": "./bin/maglev-sync",
    "dev-maglev-gen": "yarn build-maglev-gen && yarn maglev-gen",
    "dev-maglev-sync": "yarn build-maglev-sync && yarn maglev-sync"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.3.5",
    "axios": "^1.0.0",
    "nuxt": "3.0.0-rc.11"
  },
  "dependencies": {
    "ajv": "^8.11.0",
    "nuxt-helmet": "^2.1.1",
    "readdir-glob": "^1.1.2"
  }
}
